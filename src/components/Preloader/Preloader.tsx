import * as React from 'react';
import './Preloader.scss';

const Preloader = () => (
  <div className='preloader'>
    <div className='preloader__wrapper'>
      <div className='preloader__runner' />
    </div>
  </div>
);

export default Preloader;
